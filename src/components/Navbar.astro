---
import type { Lang } from '../i18n/utils';
import { useTranslations, getLocalePath, getAlternateLang } from '../i18n/utils';
import { languages } from '../i18n/ui';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
const altLang = getAlternateLang(lang);

const navLinks = [
  { href: '#how-it-works', label: t('nav.howItWorks') },
  { href: '#gallery', label: t('nav.gallery') },
  { href: '#pricing', label: t('nav.pricing') },
  { href: '#map', label: t('nav.map') },
  { href: '#faq', label: t('nav.faq') },
  { href: '#contact', label: t('nav.contact') },
];

const legalLinks = [
  { id: 'open-delete-modal', label: t('legal.deleteAccount') },
  { id: 'open-terms-modal', label: t('legal.terms') },
  { id: 'open-privacy-modal', label: t('legal.privacy') },
  { id: 'open-kvkk-modal', label: t('legal.kvkk') },
];
---

<nav
  class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-lg border-b border-slate-200/60 transition-all duration-300"
  id="navbar"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo (instant jump to top, no smooth scroll) -->
      <a href={getLocalePath(lang)} id="logo-link" class="flex items-center gap-2 group">
        <div class="w-8 h-8 bg-primary-600 rounded-lg flex items-center justify-center group-hover:bg-primary-700 transition-colors group-hover:scale-110 transition-transform duration-200">
          <svg class="w-5 h-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z" />
          </svg>
        </div>
        <span class="text-xl font-bold text-slate-900">Merbis</span>
      </a>

      <!-- Desktop Nav -->
      <div class="hidden lg:flex items-center gap-1">
        {navLinks.map((link) => (
          <a
            href={link.href}
            class="px-3 py-2 text-sm font-medium text-slate-600 hover:text-primary-600 rounded-lg hover:bg-primary-50 transition-all duration-200"
          >
            {link.label}
          </a>
        ))}

        <!-- Yasal Dropdown -->
        <div class="relative" id="legal-dropdown">
          <button
            class="px-3 py-2 text-sm font-medium text-slate-600 hover:text-primary-600 rounded-lg hover:bg-primary-50 transition-all duration-200 inline-flex items-center gap-1"
            id="legal-dropdown-btn"
          >
            {t('nav.legal')}
            <svg class="w-3.5 h-3.5 transition-transform duration-200" id="legal-dropdown-arrow" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div
            id="legal-dropdown-menu"
            class="absolute right-0 top-full mt-1 w-52 bg-white rounded-xl shadow-lg border border-slate-200 py-1.5 opacity-0 scale-95 pointer-events-none transition-all duration-200 origin-top-right"
          >
            {legalLinks.map((link) => (
              <button
                id={link.id}
                class="legal-modal-trigger w-full text-left px-4 py-2.5 text-sm text-slate-600 hover:text-primary-600 hover:bg-primary-50 transition-colors"
              >
                {link.label}
              </button>
            ))}
          </div>
        </div>
      </div>

      <!-- Right side: Lang switch + CTA + Mobile menu -->
      <div class="flex items-center gap-3">
        <!-- Language Switcher -->
        <a
          href={getLocalePath(altLang)}
          class="text-sm font-medium text-slate-500 hover:text-primary-600 transition-colors px-2 py-1 rounded border border-slate-200 hover:border-primary-300 hover:scale-105 active:scale-95 transition-transform duration-150"
        >
          {languages[altLang]}
        </a>

        <!-- CTA Button (desktop) -->
        <a
          href="#download"
          class="hidden sm:inline-flex items-center gap-2 bg-primary-600 hover:bg-primary-700 text-white text-sm font-semibold px-4 py-2 rounded-lg transition-all shadow-sm hover:shadow-md hover:-translate-y-0.5 active:translate-y-0 duration-200"
        >
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
          </svg>
          {t('nav.download')}
        </a>

        <!-- Mobile Menu Button -->
        <button
          id="mobile-menu-btn"
          class="lg:hidden p-2 rounded-lg text-slate-600 hover:bg-slate-100 transition-colors active:scale-90 transition-transform duration-150"
          aria-label="Menu"
        >
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path id="menu-icon" stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu"
    class="lg:hidden hidden border-t border-slate-200/60 bg-white/95 backdrop-blur-lg"
  >
    <div class="px-4 py-4 space-y-1">
      {navLinks.map((link) => (
        <a
          href={link.href}
          class="mobile-nav-link block px-3 py-2.5 text-base font-medium text-slate-700 hover:text-primary-600 hover:bg-primary-50 rounded-lg transition-colors"
        >
          {link.label}
        </a>
      ))}

      <!-- Mobile Legal Submenu -->
      <div class="border-t border-slate-200 pt-2 mt-2">
        <p class="px-3 py-1.5 text-xs font-semibold text-slate-400 uppercase tracking-wider">{t('nav.legal')}</p>
        {legalLinks.map((link) => (
          <button
            id={`mobile-${link.id}`}
            class="legal-modal-trigger mobile-nav-link w-full text-left block px-3 py-2.5 text-base font-medium text-slate-700 hover:text-primary-600 hover:bg-primary-50 rounded-lg transition-colors"
          >
            {link.label}
          </button>
        ))}
      </div>

      <a
        href="#download"
        class="mobile-nav-link block mt-3 text-center bg-primary-600 hover:bg-primary-700 text-white font-semibold px-4 py-3 rounded-lg transition-colors"
      >
        {t('nav.download')}
      </a>
    </div>
  </div>
</nav>

<script>
  // Mobile menu toggle
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const mobileLinks = menu?.querySelectorAll('.mobile-nav-link');

  btn?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });

  mobileLinks?.forEach((link) => {
    link.addEventListener('click', () => {
      menu?.classList.add('hidden');
    });
  });

  // Logo: instant teleport to top
  const logoLink = document.getElementById('logo-link');
  logoLink?.addEventListener('click', (e) => {
    e.preventDefault();
    document.documentElement.style.scrollBehavior = 'auto';
    window.scrollTo(0, 0);
    requestAnimationFrame(() => {
      document.documentElement.style.scrollBehavior = '';
    });
  });

  // Desktop Legal Dropdown
  const dropdownBtn = document.getElementById('legal-dropdown-btn');
  const dropdownMenu = document.getElementById('legal-dropdown-menu');
  const dropdownArrow = document.getElementById('legal-dropdown-arrow');
  let dropdownOpen = false;

  function toggleDropdown(open: boolean) {
    dropdownOpen = open;
    if (open) {
      dropdownMenu?.classList.remove('opacity-0', 'scale-95', 'pointer-events-none');
      dropdownMenu?.classList.add('opacity-100', 'scale-100');
      dropdownArrow?.classList.add('rotate-180');
    } else {
      dropdownMenu?.classList.add('opacity-0', 'scale-95', 'pointer-events-none');
      dropdownMenu?.classList.remove('opacity-100', 'scale-100');
      dropdownArrow?.classList.remove('rotate-180');
    }
  }

  dropdownBtn?.addEventListener('click', (e) => {
    e.stopPropagation();
    toggleDropdown(!dropdownOpen);
  });

  document.addEventListener('click', (e) => {
    const dropdown = document.getElementById('legal-dropdown');
    if (dropdownOpen && dropdown && !dropdown.contains(e.target as Node)) {
      toggleDropdown(false);
    }
  });
</script>
